# Optional overrides for docker compose. Copy to .env if you want to
# change defaults; the file is gitignored.

# Host port the dashboard listens on (container is always 8501).
# UI_PORT=8501

# Host directory that holds config.env, stock_data.db, and stock_failures.db.
# HOST_DATA_DIR=./data
