"""
stock_toolkit
=============
Stock market data toolkit: collection, analysis, scoring, backtesting,
alerting, and a Streamlit dashboard.

Data files (config.env, stock_data.db, data/) live in the working
directory, or in $STOCK_DIR if set — see stock_toolkit.common.
"""
